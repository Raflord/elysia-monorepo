{
  "name": "@server",
  "version": "0.0.1",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts"
    }
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/index.ts",
    "build": "bun build --compile --minify --outfile dist/server src/index.ts"
  },
  "dependencies": {
    "@better-auth/kysely-adapter": "^1.5.6",
    "@shared": "workspace:*",
    "@elysiajs/cors": "^1.4.1",
    "@elysiajs/openapi": "^1.4.14",
    "better-auth": "^1.5.6",
    "kysely": "^0.28.11",
    "pg": "^8.20.0"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.9",
    "@types/pg": "^8.18.0",
    "bun-types": "^1.3.10"
  }
}
